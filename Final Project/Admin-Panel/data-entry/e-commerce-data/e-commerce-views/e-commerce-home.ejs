<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #cfd9df 0%, #789caf 100%);
            margin: 0;
            padding: 0;
        }

        .container {
            width: 90%;
            margin: 20px auto;
            background: linear-gradient(135deg, #c3e9ca 0%, #77e08a 100%);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            color: #333333;
        }

        .search-form {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .search-form label {
            font-weight: bold;
        }

        .search-form input[type="text"],
        .search-form select {
            padding: 8px;
            border: 1px solid #cccccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        .search-form input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 8px 20px;
            cursor: pointer;
        }

        .panel {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }

        .new-product-btn {
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 8px 20px;
            cursor: pointer;
            float: right;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table,
        th,
        td {
            border: 1px solid #cccccc;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        td,
        th {
            text-align: center;
            align-items: center;
        }

        td a {
            color: #4CAF50;
            text-decoration: none;
            font-size: 15px;
            font-weight: bold;
        }

        td a:hover {
            color: #175019;
        }

        
        .nav1 {


            width: 100%;
            margin: 20px 0px;
            height: 45px;
            display: flex;
            justify-content: center;
            align-items: center;
            justify-content: space-around;
            text-decoration: none;
            font-weight: bold;
            background: linear-gradient(135deg, #c3e9ca 0%, #77e08a 100%);
        }


        .nav-left a {
            color: rgb(78, 67, 67);
            text-decoration: none;
            font-size: 18px;
            padding: 7px;
        }

        .nav-left a:hover {
            background: linear-gradient(135deg, #c3e9ca 0%, #77e08a 100%);

            transform: scale(1.05);
            color: black;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>

<body>
    <div class="nav1">
        <div class="nav-left"><a href="/dash_board">Dash Board</a></div>
        <div class="nav-left"><a href="/data-management-panel">Operation</a></div>
        <div class="nav-left"><a href="/login">LogOut</a></div>
    </div>
    <div class="container">
        <button class="new-product-btn" onclick="location.href='/e-commerce-form'">New Entry</button>
        <h2>E-Commerce Compnay</h2>

        <div class="search-form">
            <form action="/E-Commerce-search" method="GET">
                <label for="searchId">Id:</label>
                <input type="search" id="searchId" name="id">
                <label for="name">Compnay Name:</label>
                <input type="search" id="name" name="name">
                <label for="mobileNumber">Mobile Number:</label>
                <input type="search" id="mobileNumber" name="mobileNumber">
                <label for="email">Email:</label>
                <input type="search" id="email" name="email">
                <input type="submit" value="Search">
            </form>
        </div>
        <table id="searchResults">
            <tr>
                <th>Id</th>
                <th>Company Name</th>
                <th>Owner</th>
                <th>Mobile Number</th>
                <th>Email</th>
                <th>Photo</th>
                <th>Action</th>
            </tr>
            <% admin_e_commerce.forEach(item=> { %>
                <tr>
                    <td>
                        <%= item.id %>
                    </td>
                    <td>
                        <%= item.name %>
                    </td>
                    <td>
                        <%= item.person %>
                    </td>
                    <td>
                        <%= item.mobileNumber %>
                    </td>
                    <td>
                        <%= item.email %>
                    </td>
                    <td>
                        <% if (item.photo) { %>
                            <img src="data:<%= item.mime_type %>;base64,<%= item.photo.toString('base64') %>"
                                alt="Admin Photo" width="50" height="50">
                            <% } else { %>
                                No Photo
                                <% } %>
                    </td>
                    <td><a href="/e-commerce-detalies/<%= item.id %>">Details</a></td>
                </tr>
                <% }); %>
        </table>
    </div>
</body>

</html>